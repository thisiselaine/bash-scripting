#!/bin/bash

# Challenge: write a script to perform automatic downloads and conversions of streaming audio files. Given a URL, batch download streaming audio files (using "wget") and convert them on the fly. 

